#include <iostream>
using namespace std;
void firstRepeating(int arr[], int n)
{
    int *arr = new int hash[n];
    for (int i = 0; i < n; i++)
    {
    }
}
int main()
{
    int arr[] = {1, 5, 3, 4, 3, 5, 6};
    int n = sizeof(arr) / sizeof(arr[0]);
    firstRepeating(arr, n);
}